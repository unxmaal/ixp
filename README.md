# ixp
irixports packager

## Usage

Usage: ./ixp.sh <action> <portname>
      Actions:
          list = list available irixports packages
          install = install package
          clean = clean up

